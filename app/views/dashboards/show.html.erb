<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :details %>
  <%= f.text_field :details, placeholder: 'Write details of your task here' %>
  <%= f.submit "Create task!" %>
<% end %>

<%= render @tasks %>